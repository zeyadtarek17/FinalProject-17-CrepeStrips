apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: crepestrips
spec:
  selector:
    app: gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30000
  type: LoadBalancer