apiVersion: v1
kind: Service
metadata:
  name: fooditem-service
  namespace: crepestrips
spec:
  selector:
    app: fooditem-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
