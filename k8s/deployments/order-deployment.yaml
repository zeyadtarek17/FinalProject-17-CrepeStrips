apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: crepestrips
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: omaradell88/order-service
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          env:
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                configMapKeyRef:
                  name: admin-config
                  key: SPRING_DATA_MONGODB_URI
            - name: SPRING_RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: admin-config
                  key: SPRING_RABBITMQ_HOST
            - name: MONGO_USERNAME
              valueFrom:
                secretKeyRef:
                  name: admin-db-secret
                  key: username
            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: admin-db-secret
                  key: password