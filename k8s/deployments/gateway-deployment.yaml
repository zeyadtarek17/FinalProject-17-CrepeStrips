apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: crepestrips
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      containers:
        - name: gateway-service
          image: omaradell88/gateway-service
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                configMapKeyRef:
                  name: admin-config
                  key: SPRING_DATA_MONGODB_URI
            - name: SPRING_RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: admin-config
                  key: SPRING_RABBITMQ_HOST
            - name: MONGO_USERNAME
              valueFrom:
                secretKeyRef:
                  name: admin-db-secret
                  key: username
            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: admin-db-secret
                  key: password