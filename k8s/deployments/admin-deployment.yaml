apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: crepestrips
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: omaradell88/admin-service
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATA_MONGODB_URI
              valueFrom:
                configMapKeyRef:
                  name: admin-config
                  key: SPRING_DATA_MONGODB_URI
            - name: SPRING_RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: admin-config
                  key: SPRING_RABBITMQ_HOST
            - name: MONGO_USERNAME
              valueFrom:
                secretKeyRef:
                  name: admin-db-secret
                  key: username
            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: admin-db-secret
                  key: password